# Summarizing Level-1 Routes into Level-2 Backbone
---
defaults.sources.extra: [ ../../defaults.yml ]
version: 2.0.0

module: [ isis ]
isis.area: '49.0100'

groups:
  external:
    members: [ x1 ]
    isis.type: level-2

nodes:
  r1:
    isis.type: level-1
  c1:
    isis.type: level-1-2
  x1:
    isis.area: "49.0001"

links:
- group: area_100
  members: [ r1-c1, r1, r1, r1 ]
- group: external
  isis.type: level-2
  members: [ c1-x1 ]

message: |
  The "Summarizing Level-1 Routes into Level-2 Backbone" lab is ready.
  Connect to the lab devices with "netlab connect".
