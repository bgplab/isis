# Multilevel ISIS networks
---

defaults.sources.extra: [ ../../defaults.yml ]
version: 2.0.0


module: [isis]

groups:
  core:
    members: [r1, c1, c2]
    isis.area: "49.0100"

  external1:
    members: [x1]
    isis.area: "49.0001"

  external2:
    members: [x2]
    isis.area: "49.0002"

  set:
    members: [r1, c1, c2, x1, x2 ]
    isis.type: level-1-2

nodes: [r1, c1, c2, x1, x2]

links:
- r1-c1
- r1-c2
- c1-c2
- c1-x1
- c2-x2
- r1:
- r1:
- r1:

message: |
  The "Multilevel ISIS networks" lab is ready. Connect to the lab devices
  with "netlab connect".